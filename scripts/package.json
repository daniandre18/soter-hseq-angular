{
  "name": "scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "seed": "tsx seed-emulator.ts",
    "seed:catalog": "tsx seed-emulator.ts --catalog-only",
    "seed:clients": "tsx seed-emulator.ts --clients-only",
    "seed:clients:production": "tsx seed-production-clients.ts --confirm-production",
    "fetch:co-locations": "tsx fetch-co-locations.ts"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
